name: PS2 Build

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]

jobs:
  build-ps2:
    runs-on: ubuntu-latest
    container: ps2dev/ps2dev:latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install System Dependencies
        run: apk update && apk add --no-cache build-base git curl cdrkit

      - name: Set Toolchain Path
        run: |
          GCC_PATH=$(find /usr/local/ps2dev -name "ee-gcc" | head -n 1)
          echo "$(dirname $GCC_PATH)" >> $GITHUB_PATH
          echo "PS2DEV=/usr/local/ps2dev" >> $GITHUB_ENV
          echo "PS2SDK=/usr/local/ps2dev/ps2sdk" >> $GITHUB_ENV

      - name: Compile Project
        run: |
          mkdir -p bin
          make clean || true
          make

      - name: Package ISO with IRX Bundle
        run: |
          mkdir -p iso_root/ROMS
          
          # 1. Download Required Drivers (UPPERCASE for PS2 CDROM compatibility)
          curl -L https://github.com/ps2dev/ps2sdk-ports/raw/master/audsrv/irx/audsrv.irx -o iso_root/AUDSRV.IRX
          curl -L https://github.com/ps2dev/ps2sdk-ports/raw/master/usbd/irx/usbd.irx -o iso_root/USBD.IRX
          curl -L https://github.com/ps2dev/ps2sdk-ports/raw/master/usbkbd/irx/usbkbd.irx -o iso_root/USBKBD.IRX
          
          # 2. Add Game Files
          cp bin/cps2emu.elf iso_root/CPS2EMU.ELF
          # Ensure your ROM zip is present in the ROMS folder
          cp roms/*.zip iso_root/ROMS/ || true
          
          # 3. Create SYSTEM.CNF
          echo "BOOT2 = cdrom0:\CPS2EMU.ELF;1" > iso_root/SYSTEM.CNF
          echo "VER = 1.00" >> iso_root/SYSTEM.CNF
          echo "VMODE = NTSC" >> iso_root/SYSTEM.CNF
          
          # 4. Create ISO
          mkisofs -o bin/cps2emu.iso -R -J -input-charset utf8 iso_root/

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: CPS2emu-PS2-ISO
          path: bin/cps2emu.iso
